<form method="post" action="{{ path('app_product_crud_delete', {'uuid': product.uuid}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product.uuid) }}">
    <button class="btn">Delete</button>
</form>
