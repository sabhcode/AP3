{% extends 'base.html.twig' %}


{% block body %}
	<h1>Catalogue</h1>


	<ul>
		{% for category in categories %}
			<li>
				<h2>{{ category.getName() }}</h2>
				<ul>
					{% for product in category.getProducts() %}
						<li>
							{{ product.getName() }}
							-
							{{ product.getPrice() }}
							€
						</li>
					{% endfor %}
				</ul>
			</li>
		{% endfor %}
	</ul>
{% endblock %}

{% block stylesheets %}
	<link
	rel="stylesheet" href="{{ asset('css/home.css') }}">

	{# Ce CSS sera inclus dans le Twig #}
	{% set custom_css %}
	body {
						            background-color: #fff;
						        }
	{% endset %}

	{{ custom_css|inline_css }}
{% endblock %}
