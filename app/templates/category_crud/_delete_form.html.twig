<form method="post" action="{{ path('app_category_crud_delete', {'uuid': category.uuid}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category.uuid) }}">
    <button class="btn">Delete</button>
</form>
