{% extends 'base.html.twig' %}

{% block title %}Category{% endblock %}

{% block body %}
    <h1>Category</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Uuid</th>
                <td>{{ category.uuid }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_category_crud_index') }}">back to list</a>

    <a href="{{ path('app_category_crud_edit', {'uuid': category.uuid}) }}">edit</a>

    {{ include('category_crud/_delete_form.html.twig') }}
{% endblock %}
