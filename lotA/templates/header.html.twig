<header id="header-app" class="header-app">
	<div class="header-app__content">
		<div class="header-app__logo">
			<a href="{{ path('app_home') }}" title="All4Sport">
				<img src="{{ asset('media/images/header/logo.png') }}">
			</a>
		</div>
		<nav class="header-app__nav">
			<ul>
				{% set path = app.request.getPathInfo() %}
				<li>
					<a href="{{ path('app_home') }}" class="{{ path == path('app_home') ? 'on' : '' }}" title="Accueil">Accueil</a>
				</li>
				<li>
					<a href="{{ path('app_categories') }}" class="{{ path == path('app_categories') ? 'on' : '' }}" title="Catalogue">Catalogue</a>
				</li>
				{# Si l'utilisateur est connecté alors on affiche ceci #}
				{% if app.user %}
					<li>
						<a href="{{ path('app_profil') }}" class="{{ path == path('app_profil') ? 'on' : '' }}" title="Profil">Profil</a>
					</li>
					<li>
						<a href="{{ path('app_logout') }}" class="{{ path == path('app_logout') ? 'on' : '' }}" title="Déconnexion">Déconnexion</a>
					</li>
				{# Sinon ça #}
				{% else %}
					<li>
						<a href="{{ path('app_login') }}" class="{{ path == path('app_login') ? 'on' : '' }}" title="Connexion">Connexion</a>
					</li>
					<li>
						<a href="{{ path('app_register') }}" class="{{ path == path('app_register') ? 'on' : '' }}" title="Inscription">Inscription</a>
					</li>
				{% endif %}
				<li>
					<a href="{{ path('app_cart') }}" class="{{ path == path('app_cart') ? 'on' : '' }}" title="Mon panier">
						Mon panier
						<span class="order_price_value">{{ cart.formatPrice(cart.getOrderPriceHT()) }}</span>
					</a>
				</li>
			</ul>
		</nav>
	</div>
</header>
